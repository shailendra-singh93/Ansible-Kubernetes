kind: Deployment
apiVersion: apps/v1beta1
metadata:
  name: httpd
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: webservers
    spec:
      containers:
      - name: httpd-container
        image: httpd
        ports:
        - containerPort: 80
